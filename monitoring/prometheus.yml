global:
  scrape_interval: 15s
scrape_configs:
  - job_name: agentroute
    metrics_path: /metrics
    authorization:
      credentials_file: /etc/prometheus/token
    static_configs:
      - targets: [agent_service:8080]
